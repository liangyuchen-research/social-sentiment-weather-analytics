# Reproduction status

## Available material

- A supplied project report with architecture, function descriptions, analytical
  scenarios, aggregate index counts and the team contribution table.
- The original GitLab project URL.

## Unavailable material

- Harvester and API implementation files.
- Kubernetes/Fission deployment manifests and environment configuration.
- Elasticsearch mappings and complete transformation code.
- The original analytical notebooks and automated checks.
- A dependency lockfile, reproducible deployment snapshot, and public data sample.

The original GitLab repository requires authentication, and no source checkout
was available for this release. This repository therefore contains documentation
only and cannot be deployed as a service.

When the authorized original source becomes available, add a sanitized copy with
its authorship and license notices, document exact dependencies and deployment
steps, replace environment-specific addresses with configuration, and verify
the pipeline using a redistributable data sample.
